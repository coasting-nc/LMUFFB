Please proceed with the following task:

**Task: Implement Visual Design Overhaul (Dark Theme & Grid Layout)**

**Reference Documents:**
*   `docs\dev_docs\Visual Design improvement plan.md` (Contains the specific color codes, layout logic, and code snippets).

**Context:**
The current GUI is cluttered and difficult to scan due to a "wall of text" effect and distracting solid-colored headers. We are switching to a professional "Dark Flat" theme with a strict 2-column grid layout (Labels on the Left, Controls on the Right) to improve readability and visual hierarchy.

**Implementation Requirements:**

1.  **GUI Styling (`src/GuiLayer.h` & `src/GuiLayer.cpp`)**:
    *   Implement the `SetupGUIStyle()` function as described in the plan.
    *   **Crucial:** Make `SetupGUIStyle()` a **public static** method in the `GuiLayer` class so it can be unit-tested externally.
    *   Call `SetupGUIStyle()` inside `GuiLayer::Init()`.
    *   Apply the "Deep Grey" background and "Teal/Blue" accent colors specified in the plan.

2.  **Layout Refactoring (`src/GuiLayer.cpp`)**:
    *   Completely refactor `DrawTuningWindow` to use `ImGui::Columns(2)`.
    *   **Left Column:** Text Labels.
    *   **Right Column:** Widgets (Sliders, Checkboxes).
    *   Replace standard `CollapsingHeader` calls with the cleaner `TreeNodeEx` or custom section logic described in the plan to remove the "zebra striping" background effect.
    *   Ensure all existing controls (Sliders, Checkboxes, Buttons) are preserved and migrated to the new layout helpers (`FloatSetting`, `BoolSetting`).

3.  **Testing (`tests\test_windows_platform.cpp`)**:
    *   Add a new test function `test_gui_style_application()`.
    *   This test must:
        1.  Initialize a headless ImGui context (`ImGui::CreateContext()`).
        2.  Call `GuiLayer::SetupGUIStyle()`.
        3.  **Verify:** Assert that the style colors have been applied correctly (e.g., check that `ImGui::GetStyle().Colors[ImGuiCol_WindowBg].x` equals `0.12f`).
        4.  Destroy the context.
    *   This ensures the styling logic is valid and doesn't crash, even without a window.

**Deliverables:**
*   Modified `src/GuiLayer.h` (Expose `SetupGUIStyle`).
*   Modified `src/GuiLayer.cpp` (Implement Style and Grid Layout).
*   Modified `tests/test_windows_platform.cpp` (Add style verification test).
*   Updated `CHANGELOG.md`.
*   Updated `VERSION`.

**Check-list for completion:**
- [ ] `SetupGUIStyle` is implemented and public.
- [ ] `DrawTuningWindow` uses 2-column layout for all settings.
- [ ] Visual "zebra" headers are removed/replaced.
- [ ] `test_gui_style_application` passes in `tests/test_windows_platform.cpp`.
- [ ] Code compiles successfully.