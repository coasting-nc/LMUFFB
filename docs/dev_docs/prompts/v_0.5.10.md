Please proceed with the following task:

**Task: Expose Advanced Smoothing Parameters (Contextual Layout)**

**Reference Documents:**
*   `docs\dev_docs\additional smoothing sliders.md` (Contains the detailed specification and layout logic)
*   `FFBEngine.h`
*   `src\Config.h` & `src\Config.cpp`
*   `src\GuiLayer.cpp`

**Context:**
Advanced users require granular control over signal latency for specific hardware (Direct Drive) and car types. Currently, three critical time constants are hardcoded or hidden. We need to expose these as user-configurable settings, persisted in the configuration file, and placed **contextually** in the GUI next to the effects they modify (rather than in a separate "Advanced" menu).

**Implementation Requirements:**

1.  **Update `FFBEngine.h` (Physics Logic):**
    *   Add public member variables:
        *   `float m_yaw_accel_smoothing = 0.010f;` (Default 10ms).
        *   `float m_chassis_inertia_smoothing = 0.025f;` (Default 25ms).
    *   **Update `calculate_force`:**
        *   Replace the local constant `const double tau_yaw = 0.0225;` with `m_yaw_accel_smoothing`.
        *   Replace the local constant `double chassis_tau = 0.035;` with `m_chassis_inertia_smoothing`.
        *   **Refactor Gyro Logic:** Ensure `m_gyro_smoothing` is treated as a **Time Constant (seconds)** in the LPF formula (`alpha = dt / (tau + dt)`), removing any legacy logic that treated it as a 0-1 factor. Ensure default is `0.010f`.

2.  **Update `src/Config.h` & `src/Config.cpp` (Persistence):**
    *   Update `Preset` struct to include `yaw_smoothing` and `chassis_smoothing`.
    *   Update `Preset::Apply`, `Preset::UpdateFromEngine`, and add fluent setters.
    *   Update `Config::Save` and `Config::Load` to persist these new keys.
    *   Update `Config::LoadPresets`:
        *   Set **"Default (T300)"** and **"T300"** presets to:
            *   Yaw Smoothing: `0.010f`
            *   Chassis Inertia: `0.025f`
            *   Gyro Smoothing: `0.020f` (Smoother for belt drive)

3.  **Update `src/GuiLayer.cpp` (Contextual UI):**
    *   **Rear Axle Section:**
        *   Insert "Kick Response" slider immediately after "Yaw Kick".
        *   Insert "Gyro Smooth" slider immediately after "Gyro Damping".
        *   Use **Red/Green** text to indicate Latency (Red > 15ms/20ms).
    *   **Grip & Slip Estimation Section:**
        *   Insert "Chassis Inertia (Load)" slider.
        *   Use **Blue** text to indicate "Simulated" time (not lag).
    *   *Note:* Refer to the reference document for exact tooltips and ranges.

4.  **Documentation:**
    *   Update `CHANGELOG.md` (New features).
    *   Update `VERSION` (Increment to 0.5.8).

**Deliverables:**
*   Modified `FFBEngine.h`
*   Modified `src/Config.h` and `src/Config.cpp`
*   Modified `src/GuiLayer.cpp`
*   Updated `CHANGELOG.md` and `VERSION`

**Check-list for completion:**
- [ ] `FFBEngine.h`: Hardcoded `tau` constants replaced with member variables.
- [ ] `Config.cpp`: New settings save/load correctly.
- [ ] `GuiLayer.cpp`: Sliders appear contextually under their parent effects.
- [ ] `GuiLayer.cpp`: Latency text (Red/Green/Blue) is visible.
- [ ] `VERSION`: Updated to 0.5.8.